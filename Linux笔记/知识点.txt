一、
(1)命令解析器
(2)Linux快捷键
(3)Linux系统目录结构
(4)用户目录
(5)文件和目录操作
(6)文件目录的属性
(7)文件权限、用户、用户组
(8)文件的查找和检索
(9)软件的安装和卸载
(10)U盘的挂载和卸载

1、命令解析器：
   shell -- unix操作系统（shell命令在终端下敲的能够被终端识别的命令就是shell命令）
   bash -- Linux操作系统
命令解析器的本质：根据命令名字，调用对应的可执行程序。
2、Linux 快捷键
      (1)命令和路径补齐
      (2)主键盘快捷键
              1.历史命令切换：
                 历史命令：
                 向上遍历：
                 向下遍历：
              2.光标移动：
                 向左：
                 向右： 